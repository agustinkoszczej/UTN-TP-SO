export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:~/workspaces/SharedLibrary/Debug -> Tirar esto en el .bashrc
sudo chmod +x path.sh -> A lo mejor no sea necesario
source path.sh ->Sobre cada Modulo
path.sh = archivo
